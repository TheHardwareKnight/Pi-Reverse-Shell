# Pi-Reverse-Shell
Python-based reverse shell using a Raspberry Pi as a relay (“dropbox”) and Twingate for secure remote access. The Windows client makes an outbound connection to the Pi, avoiding port forwarding. Commands are sent through the Pi and executed remotely with real-time output.
